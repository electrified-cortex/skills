GH CLI Repos — manage GitHub repos via `gh repo`: create, clone, fork, sync, edit metadata, delete.

Create — always specify visibility explicitly; no default assumed:
```bash
gh repo create owner/name --public --description "desc" --clone
gh repo create owner/name --private --gitignore node --license mit
```

Clone:
```bash
gh repo clone owner/repo [dir]
```

Fork — configure upstream remote:
```bash
gh repo fork owner/repo --clone --remote-name upstream
```
`--remote-name upstream` sets original repo as `upstream` remote in cloned fork.

Sync fork with upstream:
```bash
gh repo sync [--branch branch] [--force]
```
`--force` overwrites local changes to match upstream — use with care.

Edit metadata:
```bash
gh repo edit --description "new" --homepage https://example.com
gh repo edit --visibility private
gh repo edit --default-branch main
```

Rename, archive, delete:
```bash
gh repo rename new-name
gh repo archive
gh repo unarchive
gh repo delete owner/repo --yes
```

List repos:
```bash
gh repo list [owner] --limit 50 --json name,visibility,owner --jq '.[].name'
```

Set default repo — suppresses need for `--repo` on subsequent commands:
```bash
gh repo set-default owner/repo
gh repo set-default --unset
```

Scope: `gh repo` only. Excludes: repo content (files, branches, commits — git ops), secrets, deploy keys, GitHub Apps, webhooks, integrations.
