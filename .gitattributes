# Disable line-ending normalization. Windows-first workspace; CRLF is
# native and git's autocrlf warnings ("LF will be replaced by CRLF the
# next time Git touches it") are noise for our workflow.
* -text
