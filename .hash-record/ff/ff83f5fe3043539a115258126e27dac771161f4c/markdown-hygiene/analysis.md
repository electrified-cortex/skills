# Analysis

CLEAN

MD041/first-line-heading reported by linter on line 6 (`## Input`). Not a real violation:
file begins with YAML frontmatter (`---` block), which satisfies MD041's frontmatter exception.
No other violations.
