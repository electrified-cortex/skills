# hash-record - skill index

## hash-record

Supports Content-hash-keyed durable record store Probe / Read / Write / Invalidate API Replaces audit-reports/ and.

## hash-record-index

Supports Build and refresh manifestyaml index files inside each hash directory under hash-record/ Triggers — index h.

## hash-record-manifest

Supports Compute a manifest hash for a set of files Triggers — compute manifest hash, multi-file cache key, hash-recor.

## hash-record-prune

Supports Remove orphaned hash directories from a repository's hash-record/ store Triggers — prune hash records, clean.
