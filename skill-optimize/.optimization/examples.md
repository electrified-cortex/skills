# EXAMPLES — 2026-04-30

**Severity:** LOW (deferred)

**Finding:** The qualifier output format (`APPLICABLE: yes | maybe`) lacks
a concrete example pair showing the difference between "yes" and "maybe."
All other format outputs (log rows, report files) are anchored by the
existing populated log and `.optimization/` folder. Severity calibration
is addressed by per-topic `.md` files and the new self-critique step.

**Action taken:** No change. Qualifier calibration deferred — LOW priority
given the self-critique review gate. Revisit if qualifier mis-classification
becomes a observed problem.
