# MODEL SELECTION — 2026-04-30

**Severity:** LOW (deferred)

**Finding:** Host runs at Sonnet-class but most work is deterministic file
I/O and dispatch calls. Qualifier (Haiku) and topic analysis (Sonnet) tiers
are correct. The host has one judgment step — Step 3b assessor tie-breaking
— that uses prose rules rather than a decision table. Once SKILL.md is
created (see COMPRESSIBILITY finding), test whether Haiku can follow the
host-only instructions; if yes, downgrade the host and save on orchestration
cost.

**Action taken:** No change. Flagged as follow-on to SKILL.md creation work.
