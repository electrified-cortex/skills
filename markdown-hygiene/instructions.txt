RETIRED: this file is superseded by the two-phase executor split.

Lint executor (Haiku-class):    markdown-hygiene-lint/instructions.txt
Analysis executor (Sonnet-class): markdown-hygiene-analysis/instructions.txt
Orchestrator:                     SKILL.md

Do not read further. Read SKILL.md instead.

Hard prohibition: don't author scripts (`.ps1`, `.sh`, `.py`, etc.), helper files, or any file other than `<report_path>`. Target is read-only. Use Read/Bash/Grep only for inspection.

1. Run verify, whichever runtime has:
   Bash: `bash <skill-dir>/verify.sh <markdown_file_path> [--ignore <RULE>[,<RULE>...]]`
   PS7: `pwsh <skill-dir>/verify.ps1 <markdown_file_path> [-Ignore <RULE>[,<RULE>...]]`
   Verify lists every rule it determined and result. Include its output in report findings. You (LLM) are responsible for everything verify didn't determine — covered in step 2.
2. Scan `<markdown_file_path>` for remaining rules. Read file (Read tool), apply rule-knowledge from step 3. Cross-check each finding against actual line; drop any not pointable to verified line. Skip `--ignore` rules. Cover every rule in step 3, incl. all four table rules (MD055/MD056/MD058/MD060).
3. Reference rules (use to compose `Fix:` imperatives for rules verify didn't determine):
   MD001 — heading levels increment by one (H1 to H3 violates).
   MD003 — heading style consistent (atx `#` vs setext `===`/`---`).
   MD004 — list markers consistent (`-`, `*`, `+`).
   MD022 — headings need blank before AND after (except start/end).
   MD023 — headings must not have leading whitespace.
   MD024 — duplicate heading text among siblings.
   MD025 — only one H1 per file.
   MD026 — headings must not end with `.`, `!`, `?`, `:`, `,`, `;`.
   MD029 — ordered list prefixes consistent.
   MD031 — fenced code blocks need blanks before AND after.
   MD032 — lists need blanks before AND after.
   MD033 — no inline HTML outside fenced blocks.
   MD034 — bare URLs (not in angle brackets, backticks, or links).
   MD040 — fenced code blocks need language identifier.
   MD055 — table pipe style consistent across rows.
   MD056 — all rows in table same number of cells.
   MD058 — tables need blanks before AND after.
   MD060 — table cell separators need space on each side of dash run.
4. Semantic advisory scan: read file, evaluate advisory rules below. Not lint violations — observations. Each entry uses `Note:` not `Fix:`. Host LLM decides whether to act. Skip `--ignore` rules.
   SA001 [FAIL] — 3+ consecutive ALL CAPS words not an acronym/known term.
   SA002 [FAIL] — entire sentence in ALL CAPS.
   SA003 [WARN] — same ALL CAPS word 5+ times in document (signal collapse).
   SA004 [WARN] — >~15% non-code body text is bold (emphasis diluted).
   SA005 [WARN] — bold spans 3+ consecutive sentences in same section.
   SA006 [FAIL] — single-item list (use a sentence instead).
   SA007 [WARN] — two-item list that reads naturally as `X and Y`.
   SA008 [WARN] — list nesting 3+ levels deep.
   SA009 [WARN] — list items are multi-sentence paragraphs (consider sections).
   SA010 [WARN] — file path, shell command, or env var in plain prose without backticks.
   SA011 [FAIL] — ALL CAPS and bold on same word/phrase (e.g. `**NEVER**`) — redundant double-emphasis.
   SA012 [WARN] — heading immediately followed by another heading, no content between.
   SA013 [WARN] — heading introduces only a single sentence (use `**Label:**` instead).
   SA014 [SUGGEST] — `never`/`must not`/`do not`/`always` unemphasized in instruction document.
   SA015 [WARN] — document >400 words, zero headings.
   SA016 [WARN] — heading >~60 chars.
   SA017 [WARN] — heading phrased as question (ends `?`) outside FAQ context.
   SA018 [WARN] — passive voice on directive sentence in instruction/spec document.
   SA019 [WARN] — paragraph >8 sentences.
   SA020 [WARN] — numbered list with no sequential dependency (use bullets).
   SA021 [WARN] — link text is raw URL.
   SA022 [WARN] — generic link text: "click here", "here", "this link", "read more".
   SA023 [FAIL] — empty fenced code block.
   SA024 [WARN] — backtick on plain prose word that is not code/path/command/literal.
   SA025 [WARN] — strikethrough text in non-draft document.
   SA026 [WARN] — `---`/`***` used as section divider where a heading should be used.
   SA027 [WARN] — same word in every sibling heading within a section.
   SA028 [WARN] — phrase of 5+ words or full sentence appears verbatim more than once.
   SA029 [WARN] — positional reference: "see above", "see below", "as mentioned earlier", etc.
   SA030 [WARN] — blockquote (`>`) used as callout/emphasis rather than quotation.
   SA031 [WARN] — sibling headings mix Title Case and Sentence case.
   SA032 [WARN] — same concept referred to by multiple distinct names (LLM-detected).
   SA033 [FAIL] — negation stacking in directive (e.g. "do not fail to avoid") — rewrite as positive.
   SA034 [WARN] — vague qualifier on directive ("sometimes", "usually", "often", "generally") without stating exception condition.
   SA035 [WARN] — action stated before gate condition ("Delete X if Y" — prefer "If Y, delete X").
   SA036 [WARN] — directive sentence has 3+ coordinating conjunctions — split into one instruction per sentence.
   SA037 [WARN] — list mixes command items and observation/description items without a distinguishing signal.
   SA038 [FAIL] — contradictory instructions present in document (LLM-detected).
5. Write report at `<report_path>`:
   `mkdir -p $(dirname <report_path>)` first.
   Frontmatter (`---` open/close):
     `file_path: <repo-relative path>` — repo-relative, NOT absolute. Via `git ls-files --full-name <markdown_file_path>` or strip `<repo_root>/`.
     `operation_kind: markdown-hygiene`
     `result: clean` (no violations, no advisories), `result: fail` (lint violations present), or `result: pass` (no lint violations, but advisory observations present). If both lint violations and advisories present, use `result: fail`.
   Body:
     No violations, no advisories: `# Result\n\nCLEAN`.
     Lint violations: `# Result\n\nFINDINGS\n\n- <list>`. Each entry two lines:
       `MD0XX line N: <description>`
       Indented `Fix: <imperative>` — complete, standalone, byte-precise.
     Advisory observations (append after FINDINGS if both, or sole body if only advisories): `## Advisory\n\n- <list>`. Each entry two lines:
       `SA0XX [SEVERITY] line N: <description>` (omit line N for document-level)
       Indented `Note: <observation>` — plain English, no imperative. Host LLM decides.
6. Return `done`. On failure, return `ERROR: <reason>`.

Report Format:
Body always opens with `# Result` H1.

CLEAN:

```text
# Result

CLEAN
```

FINDINGS (`result: fail`):

```text
# Result

FINDINGS

- MD022 line 7: blank line missing before heading "Configuration"
  Fix: insert blank line before line 7
- MD047: file lacks trailing newline
  Fix: append a single newline at end of file
- MD058 line 23: table missing blank line before
  Fix: insert blank line before line 23
- MD056 line 31: table row 3 has 4 cells, table has 3 columns
  Fix: split or remove the extra cell on line 31; ensure all rows have 3 cells
```

ADVISORY only (`result: pass`):

```text
# Result

## Advisory

- SA001 [FAIL] line 14: 3 consecutive ALL CAPS words "THIS IS WRONG" — not an acronym
  Note: rephrase in sentence case or replace with single bolded constraint term
- SA014 [SUGGEST]: "never" unemphasized on line 22 in instruction document
  Note: consider bold or ALL CAPS to strengthen constraint signal
```

FINDINGS with advisories (`result: fail`):

```text
# Result

FINDINGS

- MD022 line 7: blank line missing before heading "Configuration"
  Fix: insert blank line before line 7

## Advisory

- SA006 [FAIL] line 44: single-item list
  Note: convert to plain sentence — list with one item adds no structure
```

Each entry two lines: finding (`MD0XX line N: <description>`), indented `Fix:` line.

Fix line philosophy:
`Fix:` is complete, standalone imperative applied verbatim by downstream agent with no markdown-rule knowledge. Two tests: (1) human applying only `Fix:` (without finding line or rule code) produces correct result? (2) two agents produce identical edits?

Bad: `Fix: fix the table` — ambiguous.
Bad: `Fix: enforce MD058` — assumes rule knowledge.
Good: `Fix: insert blank line before line 23` — explicit, byte-precise.
Good: `Fix: split the cell on line 31 into two cells; ensure all rows have exactly 3 cells` — explicit, no rule knowledge needed.

Table fix examples:
Tables (MD055/MD056/MD058/MD060) are high-frequency violators agents often miscorrect.

```text
- MD058 line 23: table missing blank line before
  Fix: insert blank line before line 23

- MD058 line 28: table missing blank line after
  Fix: insert blank line after line 27 (between the last table row and the next non-blank line)

- MD056 line 31: table row 3 has 4 cells, header declares 3 columns
  Fix: examine line 31; either remove the trailing extra cell to make it 3 cells, or, if the cell is intentional, add a 4th column to the header on line 28 and the separator on line 29

- MD055 line 30: table separator uses ":---:" (centered) but header on line 28 is left-aligned
  Fix: change line 30 separator to `| --- | --- | --- |` to match left-aligned header style; OR change the header's alignment style consistently

- MD060 line 29: table separator uses `|---|---|---|` (no spaces)
  Fix: replace line 29 with `| --- | --- | --- |` — exactly one space on each side of every dash run
```

Every table `Fix:` names exact column count, separator format, or line numbers. Generic instructions fail the apply test.