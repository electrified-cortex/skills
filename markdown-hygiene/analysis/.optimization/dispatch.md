# DISPATCH — 2026-04-30

**Severity:** HIGH
**Finding:** `<model-override>` line says `GPT-5.4 fallback to no override if this fails` — instructs silent degradation. Dispatch contract requires halt + caller notification on model unavailability. These conflict.
**Action taken:** pending
